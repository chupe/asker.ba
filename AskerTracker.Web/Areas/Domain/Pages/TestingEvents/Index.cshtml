@page
@model AskerTracker.Areas.Domain.Pages.TestingEvents.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestingEvent[0].Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestingEvent[0].DateHeld)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.TestingEvent)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Location.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateHeld)
            </td>
            <td>
                <partial name="_EntryButtonsPartial" model="item"/>
            </td>
        </tr>
    }
    </tbody>
</table>