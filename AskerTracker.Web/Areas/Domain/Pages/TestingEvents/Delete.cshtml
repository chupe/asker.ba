@page
@model AskerTracker.Areas.Domain.Pages.TestingEvents.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TestingEvent</h4>
    <hr/>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TestingEvent.Location)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TestingEvent.Location.Location)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TestingEvent.DateHeld)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TestingEvent.DateHeld)
        </dd>
    </dl>

    <form method="post" asp-route-returnurl="@Model.ReturnUrl" asp-route-id="@Model.TestingEvent.Id">
        <input type="submit" value="Delete" class="btn btn-danger"/> |
        <a href="@Model.ReturnUrl">Back</a>
    </form>
</div>