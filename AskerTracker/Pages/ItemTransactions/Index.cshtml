@page
@model AskerTracker.Pages.ItemTransactions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].TransactionDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].OwnershipChange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].Lender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemTransaction[0].Owner)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.ItemTransaction)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OwnershipChange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lender.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owner.FirstName)
            </td>

            <partial name="_EntryButtonsPartial" model="item"/>
        </tr>
    }
    </tbody>
</table>